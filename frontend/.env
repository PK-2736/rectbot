###############################################
# Frontend (CRA) Self-host Appwrite ENV        #
###############################################
# API base (FastAPI reverse proxy origin)
REACT_APP_API_BASE_URL=https://api.pwy.rectbot.tech

# Self-host Appwrite endpoint & project (fill actual project ID)
REACT_APP_APPWRITE_ENDPOINT=https://auth.pwy.rectbot.tech/v1
REACT_APP_APPWRITE_PROJECT=YOUR_SELF_HOST_PROJECT_ID

# (Optional) Additional feature flags etc.
# REACT_APP_FEATURE_X=false

# NOTE: After changing these, you must restart "npm start" (CRA reads env at build start).
###############################################
