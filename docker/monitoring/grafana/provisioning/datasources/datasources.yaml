apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: false

  - name: Prometheus (placeholder)
    type: prometheus
    access: proxy
    url: http://localhost:9090
    editable: false
apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: false

  - name: Prometheus (placeholder)
    type: prometheus
    access: proxy
    url: http://localhost:9090
    editable: false
