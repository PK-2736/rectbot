[server]
http_addr = 0.0.0.0
http_port = 3000
# If you serve via reverse proxy/domain, keep these. They may cause 302 on local /login
domain = grafana.recrubo.net
root_url = https://grafana.recrubo.net/

[security]
# admin credentials are typically supplied via env vars GF_SECURITY_ADMIN_USER/PASSWORD
